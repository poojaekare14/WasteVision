# WasteVision
WasteVision-ML is a machine learning-based waste image classification system that classifies waste images into seven categories using image preprocessing, PCA for dimensionality reduction, and XGBoost for classification. The project also includes a Streamlit web application for real-time waste classification through image uploads.
